Import('env')

backend = SConscript('backend/SConscript')
frontend = SConscript('frontend/SConscript')

env.RunProgram(backend + frontend,
               backend, ['--dev', '--root', str(env.Dir(env['PREFIX'] + '/web'))])
